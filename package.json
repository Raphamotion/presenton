{
  "name": "presenton",
  "version": "1.0.0",
  "type": "module",
  "description": "Open-source AI presentation generator",
  "scripts": {
    "build": "cd servers/nextjs && bun install && bunx next build",
    "start": "bun run scripts/start-server.ts",
    "dev": "NODE_ENV=development bun run scripts/start-server.ts --dev"
  }
}
